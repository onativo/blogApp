<h2 class="mt-3" style="justify-content: center; display: flex; margin: auto">Editar publicação:</h2>

<div class="card mt-3" style="margin: auto; max-width: 60%">
  <div class="card-body" >
    <form action="/admin/post/edit" method="POST">
    <input type="hidden" name="id" value="{{post.id}}">
      <label for="nome">Nome:</label>
      <input type="text" id="nome" name="title" placeholder="Insira um título" class="form-control" value="{{post.nome}}">
      <label for="slug">Slug:</label>
      <input type="text" id="slug" name="slug" placeholder="Selecione uma Categoria" class="form-control mb-3" value="{{post.slug}}">
      <label for="slug">Descrição:</label>
      <input type="text" id="slug" name="description" placeholder="Slug da Categoria" class="form-control mb-3" value="{{post.slug}}">
      <label for="slug">Conteúdo:</label>
      <input type="text" id="slug" name="content" placeholder="Slug da Categoria" class="form-control mb-3" value="{{post.content}}">
      <button type="submit" class="btn btn-outline-primary">Editar Categoria</button>
    </form>
  </div>
</div>